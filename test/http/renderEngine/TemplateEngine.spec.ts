import * as Chai from "chai";

/**
 * Test for TemplateEngine.
 */
describe("TemplateEngine", () => {

  describe("render", () => {
    it("should call the `callback` function with an error if there is an error loading the template.", () => {

    });

    it("should call the `templateRenderer` function with the `template`, the `params` and the `callback` if the template has been successfully loaded.", () => {

    });
  });

});
